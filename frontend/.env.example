# API origin (no trailing slash, no /api suffix)
EXPO_PUBLIC_API_BASE_URL=http://localhost:5001

# Optional: phrase required for destructive restore in the app
# EXPO_PUBLIC_RESTORE_CONFIRM_PHRASE=RESTORE_NOW
